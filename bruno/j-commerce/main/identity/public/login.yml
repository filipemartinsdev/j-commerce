info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: https://localhost/identity/api/v1/auth/login
  body:
    type: json
    data: |-
      {
        "email": "common@gmail.com",
        "password": "common123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
