info:
  name: get users
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/identity/admin/api/v1/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
