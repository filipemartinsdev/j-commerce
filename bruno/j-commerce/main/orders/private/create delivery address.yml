info:
  name: create delivery address
  type: http
  seq: 3

http:
  method: POST
  url: https://localhost/order/api/v1/delivery-addresses?byCoordinates=true
  params:
    - name: byCoordinates
      value: "true"
      type: query
  body:
    type: json
    data: |-
      
      {
        "haveNumber": false,
        "latitude": -16.76172706126886, 
        "longitude": -49.27842031602914,
        "complement": "depois do posto ipiranga"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
