spring:
  application:
    name: microservice-product

  datasource:
    url: jdbc:h2:mem:test
    username: test
    password: test

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: create

#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
