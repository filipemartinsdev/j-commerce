info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/identity/api/v1/auth/login"
  body:
    type: json
    data: |-
      // {
      //   "email": "admin@gmail.com",
      //   "password": "admin123"
      // }

      {
        "email": "driver@gmail.com",
        "password": "driver123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
