spring:
  application:
    name: microservice-identity

  datasource:
    url: jdbc:h2:mem:test
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: create

  flyway:
    enabled: false

#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect

