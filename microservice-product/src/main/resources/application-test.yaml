spring:
  datasource:
    url: jdbc:h2:mem:test
    username: test
    password: test

  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none

  autoconfigure:
    exclude:
      - io.awspring.cloud.autoconfigure.s3.S3AutoConfiguration