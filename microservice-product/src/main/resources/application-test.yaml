spring:
  datasource:
    url: jdbc:h2:mem:test
    username: test
    password: test

  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none

  cloud:
    aws:
      endpoint: http://localhost:4566
      region:
        static: us-east-1
      credentials:
        secret-key: test
        access-key: test
      s3:
        path-style-access-enabled: true