info:
  name: register
  type: http
  seq: 5

http:
  method: POST
  url: https://localhost/identity/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "email": "admin@gmail.com",
        "password": "admin123",
        "firstName": "admin",
        "lastName": "test"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
