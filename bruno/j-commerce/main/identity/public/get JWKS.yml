info:
  name: get JWKS
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/identity/.well-known/jwks.json"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
