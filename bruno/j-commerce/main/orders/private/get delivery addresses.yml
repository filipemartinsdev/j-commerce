info:
  name: get delivery addresses
  type: http
  seq: 1

http:
  method: GET
  url: https://localhost/order/api/v1/delivery-addresses
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
