info:
  name: register
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/identity/api/v1/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "logistics@gmail.com",
        "password": "logistics123",
        "firstName": "logistics",
        "lastName": "test"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
