info:
  name: create product
  type: http
  seq: 3

http:
  method: POST
  url: https://localhost/product/admin/api/v1/products
  body:
    type: json
    data: |-
      {
        "name": "Mouse Atack Shark x11",
        "categoryId": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
